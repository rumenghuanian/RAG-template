# 自带示例语料（demo）—— 让人 clone 下来就能跑出数字
#
# 为什么要有这个：两份真实语料（课程笔记 / 菜谱）都是第三方内容，不随仓库分发，
# 于是"clone 下来跑第一条评测"只会得到一个 FileNotFoundError。
# 这个 demo 领域用**自己写的**两篇示例文档，把「装好依赖 → 出数字」这条路补上。
#
#   检索层（零 LLM 成本）：
#       EVAL_SKILL=demo python eval/run_eval.py
#       EVAL_SKILL=demo python eval/run_agent_eval.py
#
# 它同时也是「零配置接新领域」的活例子：rag_core/skills/demo/__init__.py 里
# 只有一行 RAGSkill(name="demo") —— 元数据、prompt、分块、领域身份全走默认值。
#
# 换成你自己的语料时：改 DATA_PATH 即可（其余变量都有默认值）。
DATA_PATH=./examples/demo_corpus
FILE_GLOB=**/*.md

# CPU 上整池重排约 24 秒/次检索，实际不可用；要重排请在 GPU 机器上打开并设 RERANK_DEVICE=cuda
RERANK_ENABLED=false
